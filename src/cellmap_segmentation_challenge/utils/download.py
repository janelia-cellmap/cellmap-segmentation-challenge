import os
from typing import Sequence


def download(classes: Sequence[str], min_scale: int, output_path: str | os.PathLike):
    """Download the dataset for the CellMap Segmentation Challenge.

    Args:
        classes: The classes to download.
        min_scale: The minimum scale of the images to download.
        output_path: The path to the directory where to save the dataset.
    """
    # Download the dataset
    pass

    # Extract the dataset
    pass
